<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sbru">

    <application
        android:allowBackup="true"
        android:icon="@drawable/image"
        android:label="@string/app_name"
        android:roundIcon="@drawable/image"
        android:supportsRtl="true"
        android:theme="@style/Theme.SBRU">
        <activity android:name=".ConfirmResort"></activity>
        <activity android:name=".Cancellation" />
        <activity android:name=".Payment" />
        <activity android:name=".Reservation" />
        <activity android:name=".Resort" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    <activity android:name=".Pop" android:theme="@style/CustomTheme">

    </activity>
    </application>

</manifest>